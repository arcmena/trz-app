import React from 'react';

import { Container, Title } from './styles';

export default () => {
    return (
        <Container>
            <Title>TRADES</Title>
            <div>trade-view</div>
        </Container>
    );
};
